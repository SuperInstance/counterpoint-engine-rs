[package]
name = "counterpoint-engine"
version = "0.1.0"
edition = "2021"
description = "Species counterpoint engine — first species rules, interval checking, voice leading"
license = "MIT"
keywords = ["counterpoint", "music", "voice-leading", "species", "harmony"]

[dependencies]
